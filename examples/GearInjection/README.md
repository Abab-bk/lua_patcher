GearInjection scans items in the game. Briefly speaking, its scanning strategy
is:

- Non-vanilla
- Items not currently in loot tables

It will automatically add these items to the loot tables(LeveledList).
